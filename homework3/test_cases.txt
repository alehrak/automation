1 тесткейс: проверка создания обьекта и нахождения им xml файла для парсинга
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход имя тестируемого xml(first_test_file.xml)
вместе с путем к нему
2)запуск теста
Ожидаемый результат: тест проходит, Exception не выбрасывается
итог:пройден


2 тесткейс: проверка создания обьекта и попытка нахождения им не существующего xml файла
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход имя несуществующего xml(non_existent.xml)
2)запуск теста
Ожидаемый результат: выброшенный Exception
итог:пройден


3 тесткейс: проверка простого файла xml(first_test_file.xml) с деклорацией и небольшим набором тегов
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход имя тестируемого xml(first_test_file.xml)
вместе с путем к нему
2)вызвать у созданного обьекта метод parseDocument();
3)вызвать у созданного обьекта методы getErrors().size()
Ожидаемый результат: 0
Актуальный результат: 1
итог: не пройден(заведен баг #1)


4 тесткейс: проверка файла с подключенной схемой
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход файла xml(file_with_my_schema.xml) и путь к нему
2)вызвать у созданного обьекта метод parseDocument();
3)вызвать у созданного обьекта методы getErrors().size();
Ожидаемый результат: size() > 0 (подключение схем не поддерживается)
Актуальный результат: 0
итог: не пройден

5 тесткейс: проверка файлов со спецсимволами:
    5.1 спецсимвол >
        шаги:
        1)в тестовом методе создать экземпляр класса XMLParser и передать на вход файла xml(file_with_special_characters_one.xml) и путь к нему
        2)вызвать у созданного обьекта метод parseDocument();
        3)вызвать у созданного обьекта методы getErrors().size();
        Ожидаемый результат: size() > 0 (спецсимволы не должны поддерживаться xml файлами )
        Актуальный результат: 0
        итог: не пройден
    5.2 спецсимвол <
        1)файла xml(file_with_special_characters_two.xml)
        Ожидаемый результат: size() > 0 (спецсимволы не должны поддерживаться xml файлами )
                Актуальный результат: > 0 (3)
                итог: пройден
    5.3 спецсимвол &
                    итог: не пройден
    5.4 спецсимвол ''
                    итог: не пройден
    5.5 спецсимвол ""
                    итог: не пройден

6 тесткейс: проверка файла с подключенным doctype
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход файла xml(file_with_doctype.xml) и путь к нему
2)вызвать у созданного обьекта метод parseDocument();
3)вызвать у созданного обьекта методы getErrors().size();
Ожидаемый результат: size() > 0 (подключение doctype не поддерживается)
Актуальный результат: 0
итог: не пройден


7 тесткейс: проверка файла с данными в формате CDATA
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход файла xml(file_with_cdata.xml) и путь к нему
2)вызвать у созданного обьекта метод parseDocument();
3)вызвать у созданного обьекта методы getErrors().size();
Ожидаемый результат: 0
Актуальный результат: 3
итог: не пройден

8 тесткейс: проверка файла использующего пространство имен
шаги:
1)в тестовом методе создать экземпляр класса XMLParser и передать на вход файла xml(file_with_namespaces.xml) и путь к нему
2)вызвать у созданного обьекта метод parseDocument();
3)вызвать у созданного обьекта методы getErrors().size();
Ожидаемый результат: size() > 0 (пространство имен не поддерживается)
Актуальный результат: 0
итог: не пройден